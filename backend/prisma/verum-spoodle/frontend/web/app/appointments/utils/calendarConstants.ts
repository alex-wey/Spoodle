// Show full day to ensure all appointments are visible
export const START_HOUR = 0;   // 12 AM
export const END_HOUR = 24;    // 12 AM next day
export const HOURS_PER_DAY = END_HOUR - START_HOUR;
export const MINUTES_PER_HOUR = 60;
export const PIXELS_PER_HOUR = 80; // Height of each hour row in pixels
export const PIXELS_PER_MINUTE = PIXELS_PER_HOUR / MINUTES_PER_HOUR;
